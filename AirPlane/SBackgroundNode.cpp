#include "SBackgroundNode.h"

SBackgroundNode::SBackgroundNode(const sf::Texture& texture)
{
}

SBackgroundNode::SBackgroundNode(const sf::Texture& texture, const sf::IntRect& rect)
{
}

void SBackgroundNode::drawCurrent(sf::RenderTarget& target, sf::RenderStates states) const
{
}
